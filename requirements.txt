# Database — Turso persistent SQLite (replaces Railway Volume)
libsql-experimental>=0.0.1

# Core scanner (GitHub Actions + Streamlit Cloud)
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
ta>=0.11
requests>=2.31
pytz>=2024.1

# Dashboard UI (Streamlit Cloud)
streamlit>=1.32
streamlit-autorefresh>=1.0
plotly>=5.18
feedparser>=6.0
apscheduler>=3.10

# Optional — broker integration (best-effort install, failures won't break scanner)
upstox-python-sdk; python_version>="3.9"

# Flask API server (serves data to Dhruvedge on Vercel)
flask>=3.0
gunicorn>=21.0

# Telegram AI bot
groq>=0.4.0

# TradeFlow AI (tradeflow_streamlit.py)
anthropic>=0.25.0
python-dotenv>=1.0
